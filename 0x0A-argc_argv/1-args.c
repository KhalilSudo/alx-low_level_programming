#include <stdio.h>

/**
 * main - Entry point
 *@argv: cmd line argument
 *@argc:number of argument
 * Return: Always 0 (Success)
 **/
int main(int argc, char *argv[})
{
printf("%d\n", argc);
return (0);
}
